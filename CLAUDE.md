# tt_running_app — 프로젝트 컨텍스트

## 앱 개요
날씨·미세먼지 데이터를 기반으로 **오늘/내일 운동하기 좋은 시간대를 추천**해주는 안드로이드 앱.
Flutter로 개발 중.

## 핵심 기능
- **홈 위젯 (소형)**: 오늘 운동하기 좋은 시간대 추천
- **홈 위젯 (대형)**: 오늘 + 내일 운동 추천
- 추천 기준:
  - 미세먼지 (PM2.5, PM10) 수치
  - 날씨 (기온, 강수 확률, 바람 등)

## 플랫폼
- 타겟: Android (우선)
- 프레임워크: Flutter (Dart)

## 기술 스택 (결정 예정)
- 날씨/미세먼지 API: 기상청(공공데이터포털) + 에어코리아(한국환경공단) 조합
- 안드로이드 위젯: `home_widget` 패키지 예정
- 상태관리: Riverpod (`flutter_riverpod` + `riverpod_generator`)

## 추천 알고리즘 기준

### 미세먼지 (PM2.5 기준 — 운동 중 호흡량이 많아 PM2.5 우선)
| 수치 (μg/m³) | 등급 | 추천 |
|---|---|---|
| 0~15 | 좋음 | 운동 OK |
| 16~35 | 보통 | 운동 OK |
| 36~75 | 나쁨 | 마스크 or 실내 권고 |
| 76+ | 매우나쁨 | 야외 운동 비추 |

### 기온
- 5도 이하 or 28도 이상 → 야외 운동 비추 (기본값, 설정에서 변경 가능)

### 강수 확률
- 기본 기준: 30% 이하일 때 운동 추천

## 사용자 설정
- 추천 기준은 디폴트값으로 설정되어 있고, 슬라이더로 수치 변경 가능:
  - 미세먼지 PM2.5 기준: 기본 35 μg/m³
  - 기온 하한: 기본 5도
  - 기온 상한: 기본 28도
  - 강수 확률: 기본 30%
- **위치**: GPS 자동 감지 (기본) + 수동 위치 변경 가능

## 주요 결정사항
- 위젯 크기에 따라 정보량이 달라짐 (소형=오늘, 대형=오늘+내일)
- 위치 기반 추천 (핸드폰 GPS 기준)
- 설정에서 추천 기준 커스터마이징 가능

## 현재 진행 상태
- 설정 화면 구현 완료 (feature/settings-screen 브랜치)
  - 슬라이더 기반 추천 기준 설정 (미세먼지, 기온 상하한, 강수확률)
  - 디폴트값 마커(삼각형) 표시
  - 초기화 버튼
  - SharedPreferences 저장 연동
  - 최초 실행 분기 처리 (첫 실행 → 설정화면, 이후 → 메인화면)
- 메인 화면 stub 생성 (설정 버튼만 있는 상태)

## UI/UX

### 메인 화면
- **레이아웃**: 시간별 운동 적합도 타임라인 (시간축 그래프)
- **배경**: 현재 날씨에 따라 Lottie 애니메이션 배경 변경
  - 맑음 → 햇살/구름 애니메이션
  - 흐림 → 흐린 구름 애니메이션
  - 비 → 빗방울 애니메이션
  - 눈 → 눈 내림 애니메이션
- Lottie 패키지 사용 (`lottie` pub 패키지), JSON 벡터 기반으로 가볍고 성능 영향 없음
- 무료 에셋: lottiefiles.com 활용

## 앱 플로우
1. **최초 실행 시**: 설정 화면 바로 표시 (위치, 추천 기준 ON/OFF 등 초기 설정)
2. **이후 실행 시**: 메인 화면 (오늘 운동 추천)
3. 설정은 언제든 재진입 가능

## 개발일지 작성 방식
- 매 세션 종료 시 Claude가 채팅으로 초안 작성 (깃에 저장 안 함)
- 사용자가 복사 → claude.ai 웹에서 다듬기 → 블로그 업로드
- 문체: "~습니다"체, 최소 1200자
- 제목 형식: [개발일지] tt_running_app N일차
- 세션 끝에 블로그용 캡처 권장 목록도 함께 안내

## 개발 TODO 리스트

### 완료
- [x] 프로젝트 초기 설정 (패키지, 폴더 구조, Android 권한)
- [x] 설정 화면 구현 (슬라이더, 디폴트값 마커, SharedPreferences 저장)
- [x] 최초 실행 분기 처리
- [x] 날씨/미세먼지 API 연동 (기상청 + 에어코리아)
- [x] 데이터 모델 정의 (WeatherModel, AirQualityModel, HourlyRecommendation)
- [x] 추천 엔진 (PM2.5 + 날씨 기반 등급 계산, 설정값 반영)
- [x] 메인 화면 타임라인 UI
- [x] 홈 위젯 (소형/대형, SharedPreferences 연동, 30분 백그라운드 자동 갱신)

### 진행 예정 (순서대로)

#### 다음 작업
- [ ] 위젯 UI 다듬기 (색상·아이콘 추가)
- [ ] 오프라인/에러 시 마지막 성공 데이터 캐시 표시

#### feature/home-widget
- [ ] 소형 위젯 (오늘 추천)
- [ ] 대형 위젯 (오늘 + 내일 추천)
- [ ] 위젯 자동 갱신 주기 설정

## 다음 세션 시작 시 할 일 (Claude가 사용자에게 안내할 것)
- 현재 브랜치: `feature/api-integration`
- API 키 발급 확인 후 날씨/미세먼지 연동 시작
- 기상청: 동네예보 (초단기실황·초단기예보·단기예보)
- 에어코리아: 측정소별/시도별 실시간 측정정보
